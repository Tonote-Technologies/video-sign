
export const time_slots = (state) => {
  return state.time_slots;
};

export const country = (state) => {
  return state.country;
};

export const states = (state) => {
  return state.states;
};

export const schedule_transaction_id = (state) => {
  return state.transaction_id;
};

export const schedule_details = (state) => {
  return state.schedule_details;
};

export const virtual_session_details = (state) => {
  return state.virtual_session_details;
};

export const schedule_formdata = (state) => {
  return state.schedule_formdata;
};

// agora
export const agora = (state) => {
  return state.agora;
};

export const affidavits = (state) => {
  return state.affidavits;
};

export const notary_request = (state) => {
  return state.notary_request;
};

export const video_sign = (state) => {
  return state.video_sign;
};
